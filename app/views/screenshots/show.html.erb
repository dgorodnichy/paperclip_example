<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @screenshot.title %>
</p>
<%= image_tag @screenshot.picture.url(:medium) %>


<%= link_to 'Edit', edit_screenshot_path(@screenshot) %> |
<%= link_to 'Back', screenshots_path %>
